
export default function User({ name, role, salary, email, cp }) {
// const { name, role, salary, email, cp }=props;
 
// export default function User({ name, role, salary, email, cp }) {
  return (
    <div>
      <h1>Name :{name}</h1>
      <h1>ROle : {role}</h1>
      <h1>Salary : {salary}</h1>
      <h1>Email : {email}</h1>
      <h1>CP : {cp}</h1>
      <h1>CP : {cp}</h1>
      <h1>CP : {cp}</h1>
    </div>
  );
}
